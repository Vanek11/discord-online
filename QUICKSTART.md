# Quick Start Guide

## Немедленно запуск (Development)

### 1. Установить зависимости

```bash
# Backend
cd apps/backend
npm install

# Frontend
cd ../frontend
npm install
```

### 2. Запустить оба приложения

**Terminal 1 (Backend - WebSocket signaling server):**
```bash
cd apps/backend
npm run dev
```

Backend будет listening на `ws://localhost:3001`

**Terminal 2 (Frontend - Next.js):**
```bash
cd apps/frontend
npm run dev
```

Frontend будет доступен на `http://localhost:3000`

### 3. Открыть в браузере

Откройте 2+ вкладки/окна браузера с адресом `http://localhost:3000`

### 4. Тестирование

**Шаг 1:** Введите одну комнату (например "test-room") в обеих вкладках
```
Room: test-room
```

**Шаг 2:** Нажмите "Connect" — должны увидеть `✓ Connected`

**Шаг 3:** В одной вкладке выберите качество и нажмите "▶ Start Share" → выберите экран/окно

**Шаг 4:** В других вкладках должны увидеть трансляцию в реальном времени

**Шаг 5:** Смотрите статистику в реальном времени:
```
📊 Bitrate: 5.2 Mbps | FPS: 30
```

---

## Docker запуск

```bash
docker-compose up
```

Откройте:
- Frontend: http://localhost:3000
- Health: http://localhost:3001/health

---

## Проверить статус

### Backend здоров?
```bash
curl http://localhost:3001/health
```

Результат:
```json
{"ok":true}
```

### Frontend доступен?
```bash
curl http://localhost:3000
```

---

## Основные горячие клавиши

| Действие | Кнопка |
|----------|--------|
| Подключиться в комнату | "Connect" |
| Начать трансляцию | "▶ Start Share" |
| Остановить трансляцию | "⏹ Stop Share" |
| Выбрать качество | Dropdown (1080p/1440p/2160p) |

---

## Troubleshooting

| Проблема | Решение |
|----------|---------|
| **Нет соединения** | Проверьте что backend запущен на 3001 и frontend на 3000 |
| **"Connect" не работает** | Проверьте консоль браузера (F12 → Console) на ошибки |
| **Видео не показывается** | Дайте браузеру доступ к экрану; проверьте файервол |
| **Плохое качество** | Снизьте разрешение (1080p); проверьте сеть |

---

## Логирование

### Backend логи
```bash
cd apps/backend
npm run dev
# Выведет: "Signaling server listening on port 3001"
```

### Frontend браузер логи
Откройте DevTools (F12 → Console) для вывода WebSocket и WebRTC сообщений

---

## Production Deployment

Смотрите раздел "Развёртывание" в главном README.md
